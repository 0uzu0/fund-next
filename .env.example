# Docker Compose 部署用环境变量示例
# 复制为 .env 后按需修改，注意：值不要使用 ${...}，直接写明文

# Action 打包的镜像地址（必填）
DOCKER_IMAGE=ghcr.io/你的用户名/仓库名:latest

# Session 密钥（必填，用于登录态加密）
# 直接写一串随机字符串或 UUID，不要写成 ${SESSION_SECRET:xxx}
SESSION_SECRET=your-random-secret-key-or-uuid

# 可选：时区
# TZ=Asia/Shanghai
